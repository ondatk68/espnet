# Configuration files

Place corpus- and experiment-specific speech-tokenizer training YAML files in
this directory. GAN configurations use `gan_speech_tokenizer_train`; future
non-adversarial configurations use `speech_tokenizer_train`.
